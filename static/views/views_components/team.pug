mixin team(name, nation, achievements, photo, src)
    .team
        .name #{name}
        .img
            img(src="/img/f2teams/" + photo , alt="photo")
            h3 #{src}
        .info
            .nation Kraj: #{nation}
            .achievements Osiągnięcia: #{achievements}
