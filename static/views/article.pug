extends layout

block css
        link(rel='stylesheet' href='/STYLE/style_article.css')


block content
    #main
        #article

            h1#title #{article.title}

            p#author #{article.author}, #{article.date}

            p#short #{article.short}

            img#cover.cover(src='/upload/' + article.ID + '/cover.png' onerror="standby(this)") 

            p#text #{article.body}
        #seeAlso
            .con-title Zobacz także
            each val in [0,1,2,3]
                a.square(href="/article/" + articles[val].ID)
                    img.img.cover(src='/upload/' + articles[val].ID + '/cover.png' alt='' onerror="standby(this)")
                    .notImg
                        .title=  articles[val].title 
                        .text= articles[val].short
                        .next Czytaj dalej -->



block scripts
    script(src='/js/artykul_text.js')

